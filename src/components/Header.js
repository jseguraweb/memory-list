import React from 'react';
import '../scss/Header.scss'
import Form from './Form';

const Header = () => {
    return (
        <header>
            <h1>MEMO BOARD</h1>
            <Form />
        </header>
    );
}

export default Header;
